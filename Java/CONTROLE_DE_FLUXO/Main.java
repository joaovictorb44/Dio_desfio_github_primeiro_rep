package CONTROLE_DE_FLUXO;

public class Main {
    public static void main(String[] args){

        /*Para o vontrole de fluxo pode-se usar a estrutura de if / else e case
         * Exemplo de ternario: String idade = var >= 18 ? "adulto" : var < 18 && var >= 13 ? "adolecente" : "criança"
         * 
         * 
         * 
         */
        
    }
}
